#include<stdio.h>

/**
 * main - prints the lowercase alphabet in reverse.
 * followes by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;

	for (ch = '2'; ch >= 'a'; ch--)
	[
		putchar9ch);

	}
	putchar('\n');
	return (0);
}
