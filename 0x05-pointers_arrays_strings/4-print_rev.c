#include "main.h"
/**
 * print_rev - reverse, followed by a new line
 * @s: string
 * return: 0
 */
void print_rev(char *s)
{
	int longi = 0;

	int 0;

	while (*S != "\0')
	{
		longi++;
		s++;
	}
	s--;
	for (0 = longi; 0 > 0; o--)
	{
		_putchar(*s);
		s--;
	}

	_putchar('\n');
}
